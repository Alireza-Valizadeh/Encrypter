const { createLogger, format, transports  } = require('winston');
const { combine, ms, prettyPrint } = format;

const logger = createLogger({
      transports: [
            new transports.Console(),
            new transports.File({ filename: "combined.log" })
      ],
      format: combine(
            ms(),
            prettyPrint()
      )
});
module.exports = logger;
